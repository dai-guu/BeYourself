<% if current_user.following?(user) %>
  <%= link_to "フォロー外す", user_relationships_path(user.id), class: 'btn btn-sm btn-info btn-block', method: :delete %>
<% else %>
  <%= link_to "フォロー", user_relationships_path(user.id), class: 'btn btn-sm btn-success btn-block', method: :post %>
<% end %>