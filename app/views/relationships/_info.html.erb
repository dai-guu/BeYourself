<% if current_user.following?(user) %>
  <%= link_to "フォロー中", user_relationships_path(user.id), class: 'btn btn-sm btn-info btn-block', method: :delete %>
<% else %>
  <%= link_to "フォローする", user_relationships_path(user.id), class: 'btn btn-sm btn-outline-info btn-block', method: :post %>
<% end %>